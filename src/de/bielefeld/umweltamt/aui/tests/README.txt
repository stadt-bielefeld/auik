The package "tests" is the old JUnit test package which will be replaced bit by
bit by the folder "test" which is on the same level as the src folder and has
the same package structure as the src folder.
 