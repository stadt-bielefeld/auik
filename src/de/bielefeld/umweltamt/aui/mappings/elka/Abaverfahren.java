/**
 * Copyright 2005-2042, Stadt Bielefeld
 *
 * This file is part of AUIK (Anlagen- und Indirekteinleiter-Kataster).
 *
 * AUIK is free software: you can redistribute it and/or modify it under
 * the terms of the GNU General Public License as published by the
 * Free Software Foundation, either version 2 of the License, or (at your
 * option) any later version.
 *
 * AUIK is distributed in the hope that it will be useful, but WITHOUT
 * ANY WARRANTY; without even the implied warranty of MERCHANTABILITY or
 * FITNESS FOR A PARTICULAR PURPOSE.  See the GNU General Public
 * License for more details.
 *
 * You should have received a copy of the GNU General Public
 * License along with AUIK. If not, see <http://www.gnu.org/licenses/>.
 *
 * AUIK has been developed by Stadt Bielefeld and Intevation GmbH.
 */

// Generated by Hibernate Tools 5.0.0.Final

package de.bielefeld.umweltamt.aui.mappings.elka;

import de.bielefeld.umweltamt.aui.mappings.DatabaseAccess;
import de.bielefeld.umweltamt.aui.mappings.DatabaseClassToString;
import de.bielefeld.umweltamt.aui.mappings.DatabaseQuery;
import de.bielefeld.umweltamt.aui.mappings.DatabaseSerialVersionUID;
import de.bielefeld.umweltamt.aui.mappings.oberflgw.ZEntwaessgrAbwasbehverf;
import de.bielefeld.umweltamt.aui.utils.AuikLogger;

import com.fasterxml.jackson.annotation.JsonBackReference;
import com.fasterxml.jackson.annotation.JsonIgnore;

import java.math.BigInteger;
import java.util.Date;
import java.util.HashSet;
import java.util.List;
import java.util.Set;

/**
 * A class that represents a row in the Abaverfahren database table.<br>
 * This class is meant to serve as a model and should be copied into the
 * appropriate package and may be customized below the given mark.
 */
public class Abaverfahren  implements java.io.Serializable {

    /** Generated serialVersionUID for Serializable interface */
    private static final long serialVersionUID =
        DatabaseSerialVersionUID.forAbaverfahren;
    
    /* Primary key, foreign keys (relations) and table columns */
    private Integer nr;
    private BigInteger origNr;
    private Date aktualDat;
    private Date erstellDat;
    private String bezeichnung;
    private Set<ZEntwaessgrAbwasbehverf> ZEntwaessgrAbwasbehverfs = new HashSet<ZEntwaessgrAbwasbehverf>(0);
    private Set<ZAbaVerfahren> ZAbaVerfahrens = new HashSet<ZAbaVerfahren>(0);

    /** Logging */
    private static final AuikLogger log = AuikLogger.getLogger();

    /** Default constructor */
    public Abaverfahren() {
        // This place is intentionally left blank.
    }

    /** Minimal constructor */
    public Abaverfahren(
        Integer nr) {
        this.nr = nr;
    }

    /** Full constructor */
    public Abaverfahren(
        Integer nr, Date aktualDat, Date erstellDat, String bezeichnung, Set<ZEntwaessgrAbwasbehverf> ZEntwaessgrAbwasbehverfs, Set<ZAbaVerfahren> ZAbaVerfahrens) {
        this.nr = nr;
        this.aktualDat = aktualDat;
        this.erstellDat = erstellDat;
        this.bezeichnung = bezeichnung;
        this.ZEntwaessgrAbwasbehverfs = ZEntwaessgrAbwasbehverfs;
        this.ZAbaVerfahrens = ZAbaVerfahrens;
    }

    /* Setter and getter methods */
    public Integer getNr() {
        return this.nr;
    }

    public void setNr(Integer nr) {
        this.nr = nr;
    }

    public Date getAktualDat() {
        return this.aktualDat;
    }

    public void setAktualDat(Date aktualDat) {
        this.aktualDat = aktualDat;
    }

    public Date getErstellDat() {
        return this.erstellDat;
    }

    public void setErstellDat(Date erstellDat) {
        this.erstellDat = erstellDat;
    }

    public String getBezeichnung() {
        return this.bezeichnung;
    }

    public void setBezeichnung(String bezeichnung) {
        this.bezeichnung = bezeichnung;
    }

    @JsonBackReference
    public Set<ZEntwaessgrAbwasbehverf> getZEntwaessgrAbwasbehverfs() {
        return this.ZEntwaessgrAbwasbehverfs;
    }

    public void setZEntwaessgrAbwasbehverfs(Set<ZEntwaessgrAbwasbehverf> ZEntwaessgrAbwasbehverfs) {
        this.ZEntwaessgrAbwasbehverfs = ZEntwaessgrAbwasbehverfs;
    }

    @JsonBackReference
    public Set<ZAbaVerfahren> getZAbaVerfahrens() {
        return this.ZAbaVerfahrens;
    }

    public void setZAbaVerfahrens(Set<ZAbaVerfahren> ZAbaVerfahrens) {
        this.ZAbaVerfahrens = ZAbaVerfahrens;
    }

    /**
     * To implement custom toString methods, jump to not generated code.<br>
     * Basically we either call on <code>toDebugString</code> for a debug
     * string, call on <code>toGuiString</code> for a gui representation or do
     * something completely different.
     * @return String
     */
    @Override
    public String toString() {
        return DatabaseClassToString.toStringForClass(this); 
    }

    /**
     * Get a string representation for debugging
     * @return String
     */
    public String toDebugString() {
        StringBuffer buffer = new StringBuffer();
        
        buffer.append(getClass().getSimpleName()).append("@").append(Integer.toHexString(hashCode())).append(" [");
        buffer.append("nr").append("='").append(getNr()).append("' ");			
        buffer.append("aktualDat").append("='").append(getAktualDat()).append("' ");			
        buffer.append("erstellDat").append("='").append(getErstellDat()).append("' ");			
        buffer.append("bezeichnung").append("='").append(getBezeichnung()).append("' ");			
        buffer.append("ZEntwaessgrAbwasbehverfs").append("='").append(getZEntwaessgrAbwasbehverfs()).append("' ");			
        buffer.append("ZAbaVerfahrens").append("='").append(getZAbaVerfahrens()).append("' ");			
        buffer.append("]");

        return buffer.toString();
    }

    /**
     * @param other
     * @return <code>true</code>, if this and other are equal,
     *         <code>false</code> otherwise
     */
    @Override
    public boolean equals(Object other) {
        if (this == other) return true;
        if (other == null) return false;
        if (!(other instanceof Abaverfahren)) return false;
        return (this.getNr().equals(
            ((Abaverfahren) other).getNr()));
    }

    /**
     * Calculate a unique hashCode
     * @return <code>int</code>
     */
    @Override
    public int hashCode() {
        int result = 17;
        int idValue = this.getNr() == null ?
            0 : this.getNr().hashCode();
        result = result * 37 + idValue;
        return result;
    }
    
    /**
     * Merge (save or update) a detached instance
     * @param detachedInstance the instance to merge
     * @return <code>Abaverfahren</code> the merged instance,
     *         if everything went okay,
     *         <code>null</code> otherwise
     */
    public static Abaverfahren merge(Abaverfahren detachedInstance) {
        log.debug("Merging Abaverfahren instance " + detachedInstance);
        return (Abaverfahren) new DatabaseAccess().merge(detachedInstance);
    }

    /**
     * Merge (save or update) this instance
     * @return <code>true</code>, if everything went okay,
     *         <code>false</code> otherwise
     */
    public boolean merge() {
        Abaverfahren saved = Abaverfahren.merge(this);
        if (saved == null) {
            return false;
        } else {
            this.copy(saved);
            return true;
        }
    }

    /**
     * Update this Abaverfahren with its new values.<br>
     * This is meant to be used after merging!
     * @param copy Abaverfahren
     */
    private void copy(Abaverfahren copy) {
        this.nr = copy.getNr();            
        this.aktualDat = copy.getAktualDat();            
        this.erstellDat = copy.getErstellDat();            
        this.bezeichnung = copy.getBezeichnung();            
        this.ZEntwaessgrAbwasbehverfs = copy.getZEntwaessgrAbwasbehverfs();            
        this.ZAbaVerfahrens = copy.getZAbaVerfahrens();            
    }    

    /**
     * Delete (mark as deleted) a detached instance
     * @param detachedInstance the instance to delete
     * @return <code>true</code>, if everything went okay,
     *         <code>false</code> otherwise
     */
    public static boolean delete(Abaverfahren detachedInstance) {
        log.debug("Deleting Abaverfahren instance " + detachedInstance);
        return new DatabaseAccess().delete(detachedInstance);
    }

    /**
     * Delete (mark as deleted) this instance
     * @return <code>true</code>, if everything went okay,
     *         <code>false</code> otherwise
     */
    public boolean delete() {
        return Abaverfahren.delete(this);
    }

    /**
     * Find an <code>Abaverfahren</code> instance by its primary key
     * @param id the primary key value
     * @return <code>Abaverfahren</code> the requested instance,
     *         if one exists,
     *         <code>null</code> otherwise
     */
    public static Abaverfahren findById(java.lang.Integer id) {
        log.debug("Getting Abaverfahren instance with id: " + id);
        return (Abaverfahren)
            new DatabaseAccess().get(Abaverfahren.class, id);
    }

    /**
     * Get a list of all <code>Abaverfahren</code>
     * @return <code>List&lt;Abaverfahren&gt;</code>
     *         all <code>Abaverfahren</code>
     */
    public static List<Abaverfahren> getAll() {
        return DatabaseQuery.getAll(new Abaverfahren());
    }

    /**
     * Get a list of all <code>Abaverfahren</code>
     * @return <code>List&lt;Abaverfahren&gt;</code>
     *         all <code>Abaverfahren</code>
     */
    public static List<Abaverfahren> getNwBehandel() {
        return DatabaseQuery.getNwAbaVerfahrens();
    }

    /* Custom code goes below here! */
    public String toGuiString() {
        return getBezeichnung();
    }

    public void setOrigNr(BigInteger origNr) {
        this.origNr = origNr;
    }

    @JsonIgnore
    public BigInteger getOrigNr() {
        return this.origNr;
    }
}
